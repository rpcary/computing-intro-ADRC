The files and folders in this directory are write-protected. In order
 to follow along with the tutorials please copy this directory to 
 your home directory (or directory of your choice) so that you can 
 safely execute the programs there. 
To copy this directory use the following commands:
[user@computer]$ mkdir ~/teaching
[user@computer]$ cp -r /mounts/data/analyses/rpcary/teaching/Section2 ~/teaching/Section2

If you need to reset your copy after changing it, use an rsync...
[user@computer]$ rsync -rv /mounts/data/analyses/rpcary/teaching/Section2 ~/teaching/Section2
